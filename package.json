{
  "name": "baseball-games",
  "private": true,
  "description": "Coherency Baseball Games — all games in one repo",
  "scripts": {
    "build": "npm run build:statpad && npm run build:deadeye",
    "build:statpad": "cd games/statpad && npm ci && npm run build",
    "build:deadeye": "cd games/deadeye && npm ci && npm run build",
    "dev:statpad": "cd games/statpad && npm run dev",
    "dev:deadeye": "cd games/deadeye && npm run dev"
  }
}
